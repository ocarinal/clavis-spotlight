# 关于这个项目

## 一句话

把 Clavis 的 Spotlight 单独拿出来当日常启动器：按 **Super+S** 呼出一块悬浮面板，
直接输入就是搜索，不输入就是应用网格。

## 为什么会有它

- 想要接近 macOS Spotlight 的手感：一键呼出、输入即搜、结果按类型分组。
- 桌面壁纸、桌面配色、状态栏和 dock 仍然由 Noctalia 负责，启动器不该跟它抢职责。
- 因此这里使用**隔离配置**运行，自己的主题、偏好、历史互不影响；
  只把「换壁纸」和「取色」这两件事委托给 Noctalia。
- 启动器属于每天要按很多次的东西，所以从设计上要求：重启、休眠、进程崩溃之后
  仍然随时能呼出来（详见 README 第 5 节的保活机制）。

## 能做什么

- **统一搜索**：输入关键词后，结果分为 应用 / 设置 / 操作 / 壁纸 四组，
  末尾还有「在文件中搜索…」「在网页中搜索…」两条行动项。
- **四个圆按钮**：网络搜索、壁纸、剪贴板、文件；按 ESC 逐层退回，不会一下子关掉面板。
- **网络搜索**：点胶囊上的引擎名即可在 Google / Bing / 百度 / DuckDuckGo / Yahoo / Yandex / Brave
  之间切换，选择会被记住，首页搜索沿用同一引擎。
- **壁纸**：点一下立即生效，面板自己的配色跟随壁纸重新生成。
- **剪贴板与文件**：历史记录、搜索、打开或定位，走 key-cli 后端。
- **整理应用**：右键置顶 / 隐藏 / 恢复显示 / 还原自动排序，长按约半秒可拖动排序。
- **自带 dock**：底部一条与 Spotlight 同款配色和背景模糊的圆角 dock，
  鼠标移上去图标放大、启动时弹跳、悬停显示窗口预览，支持拖入固定与右键操作。
- **外观**：深色模式下的液态玻璃质感、四圆展开动画、图标入场动画。

## 由什么组成

| 部分 | 说明 |
| --- | --- |
| `upstream/` | 修改过的 Clavis 源码快照（上游 `StatIndet/quickshell`，基线提交 `4709c87`） |
| `spotlight-toggle` | 启动与保活脚本，安装为 `~/.local/bin/clavis-spotlight` |
| `state/` | 隔离的个人状态：语言、置顶/隐藏/排序、搜索引擎、当前壁纸等 |
| `install.sh` | 一键部署：依赖检查 → 构建 → 安装脚本 → 写入 niri 自启动 → 安装 dock 快捷方式 |
| `docs/noctalia-integration.md` | Noctalia 侧需要手动恢复的三处设置 |
| Noctalia（外部） | 桌面壁纸、桌面配色、状态栏与 dock |
| key-cli（外部） | 剪贴板历史与文件搜索后端 |

## 来源与许可

本仓库是基于上游 Clavis（`StatIndet/quickshell`）的**个人修改快照**，
上游及其依赖的许可证随源码保留（见 `upstream/LICENSE`、`upstream/licenses/`）。
它不是上游官方发行版，问题与改动都由使用者自行维护。

## 从哪里开始

- 想部署或系统重装后恢复：看 [`README.md`](README.md) 第 3 节。
- 想了解保活与自愈：看 [`README.md`](README.md) 第 5 节。
- 想恢复 Noctalia 那三处设置：看 [`docs/noctalia-integration.md`](docs/noctalia-integration.md)。
